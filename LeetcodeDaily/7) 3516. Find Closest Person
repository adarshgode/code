class Solution {
    public int findClosest(int x, int y, int z) {
        int stepsx = Math.abs(z-x);
        int stepsy = Math.abs(z-y);
        if(stepsx == stepsy) return 0;
        else if(stepsx<stepsy) return 1;
        return 2;
    }
}
